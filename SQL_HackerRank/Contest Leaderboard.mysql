SELECT Hacker_id, Name, SUM(Score) S
FROM(
SELECT A.HACKER_ID Hacker_id, B.NAME Name, MAX(SCORE) Score
FROM SUBMISSIONS AS A LEFT JOIN HACKERS AS B ON A.HACKER_ID=B.HACKER_ID
GROUP BY A.HACKER_ID,B.NAME, A.CHALLENGE_ID
) C
GROUP BY Hacker_id,Name
HAVING SUM(Score)>0
ORDER BY S DESC, Hacker_id
